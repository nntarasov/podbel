1:1:2:3:5:8:13:

В классе определён метод:
<code>
static IEnumerable<int> Figs(int cnt){
	for (int і = О, prev = 1, cur = 1; i < cnt; i++){
		yield return prev;
		int neww = prev + cur;
		prev = cur;
		cur = new;
	}
}
</code>
В результате выполнения фрагмента кода в методе того же класса:
<code>
foreach (int fig in Figs(7))
	Console.Write(fig+":");
</code>
на экран будет выведено:
<i>Примечание:
Если возникнет ошибка компиляции, введите: ***
Если ошибок и исключений нет, но на экран не выведится ничего, введите: ---
Если возникнет ошибка исполнения или исключение, введите: +++</i>