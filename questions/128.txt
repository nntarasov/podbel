3;

Возможный корректный порядок действий в контролируемом коде, если возникает исключение, таков: Выполняется блок контроля за исключениями. При возникновении исключения...
1) управление незамедлительно передается первому блоку <code>catch</code>
2) сначала заканчивается выполнение блока <code>try</code>, затем управление передается блоку <code>catch</code>, настроенному на обработку соответствующего исключения
3) управление незамедлительно передается блоку <code>catch</code>, настроенному на обработку соответствующего исключения
4) управление незамедлительно передается блоку <code>finally</code>, минуя блоки <code>catch</code>
5) управление незамедлительно передается блоку <code>catch</code>, настроенному на обработку соответствующего исключения, блок <code>finally</code> игнорируется