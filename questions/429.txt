1;4;

Укажите номера верных высказываний:
l) Оператор <code>where</code> задает ограничение на типизирующий параметр
2) Каждый оператор <code>where</code> завершается точкой с запятой
3) В одном операторе <code>where</code> всегда можно указать ограничения, налагаемые на все параметры обобщения
4) В одном операторе <code>where</code> можно указать несколько ограничений, накладываемых на один параметр обобщения
5) Операторы <code>where</code> должны размещаться в том порядке, в котором расположены параметры обобщения.