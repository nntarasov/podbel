2;3;5;

<i>Над строками пределы операции:</i>
1) приведение символов строки к верхнему регистру (<code> ^ </code>)
2) конкатенция строк (<code> + </code>)
3) присваивание (<code> = </code>)
4) инвертация строки (<code> ~ </code>)
5) обращение к символу строки по индексу (<code> [ ] </code>)