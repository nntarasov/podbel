3;4;5;

<b>Верно</b>, что в объявлении метода:
1) параметры передаются только по ссылке
2) тип возвращаемого значения может отсутствовать
3) допускается только один модификатор <code>params</code>
4) в спецификации параметров допускаются модификаторы <code>params</code>, <code>out</code>, <code>ref</code>
5) спецификация параметров может быть пустой