2;3;4;

Секция <code>from</code> в LINQ-запросе
1) может быть только одна;
2) вводит переменную итераций;
3) определяет источник данных -- перебираемую последовательность;
4) обязательно должна присутствовать в начале;
5) может включать только одну секцию <code>where</code>.