2;3;4;

Укажите все действия, входящие в процедуру создания нового потока:
1) подключить метод к экземпляру делегата <code>ThreadsStart</code>
2) объявить переменную типа <code>Thread</code>
3) создать обьект класса <code>Thread</code>
4) вызвать метод <code>Start()</code> для объекта типа <code>Thread</code>
5) вызвать статический метод <code>Start()</code> для класса Thread