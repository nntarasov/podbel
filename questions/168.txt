1;2;4;5;

Верно, что в абстрактном классе могут быть описаны:
1) не абстрактные методы
2) открытые (<code>public</code>) члены
3) виртуальные поля
4) не абстрактные поля
5) защищённые (<code>protected</code>) члены