1;

Для хранения значения переменной типа <code>sbyte</code> в языке С# выделяется память, размером:
1) 1 байт
2) 2 байта
3) 1 байт для хранения положительных чисел, 2 байта для хранения отрицательных
4) 2 байт для хранения положительных чисел, 4 байта для хранения отрицательных
5) 4 байта